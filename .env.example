# LLM / embeddings (Phase 0 uses OpenAI by default when set)
OPENAI_API_KEY=
# Optional — Phase 0+ may use Claude when provider=anthropic
ANTHROPIC_API_KEY=

# Paths (defaults: ./data and ./meta under project root)
# RAG_LAB_DATA_DIR=
# RAG_LAB_META_DIR=

# Streamlit
# RAG_LAB_HOST=127.0.0.1
# RAG_LAB_PORT=8505

# RAG defaults
# RAG_LAB_PROVIDER=openai          # openai | anthropic | mock
# RAG_LAB_CHAT_MODEL=gpt-4o-mini
# RAG_LAB_EMBED_MODEL=text-embedding-3-small
# RAG_LAB_TOP_K=4
# RAG_LAB_CHUNK_SIZE=800
# RAG_LAB_CHUNK_OVERLAP=120
# RAG_LAB_MAX_UPLOAD_MB=25
